{
  "name": "spirit_swap",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "auth": "graph auth https://api/thegraph.com/deploy/",
    "prep:opera:mainnet": "mustache networks/mainnet_opera.json ./templates/subgraph.template.yaml > subgraph.yaml && yarn codegen;",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ evele/spirit-swap-subgraph-dev-v2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0",
    "mustache": "^4.1.0"
  }
}
